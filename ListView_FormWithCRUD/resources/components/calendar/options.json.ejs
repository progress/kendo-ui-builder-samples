{
    widget: null,
    options: {
        min: new Date("<%= minDate %>"),
        max: new Date("<%= maxDate %>"),
        format: "<%= format %>"<% if (typeof defaultValue !== "undefined") { %>, 
        value: new Date("<%= defaultValue %>")<% } %>
    },
    events: {
        onChange: function(e) {
            <% if (events.onChange) { %>
                 $scope.factory['<%- events.onChange %>'](e);
            <% } %>
        }
    }
}
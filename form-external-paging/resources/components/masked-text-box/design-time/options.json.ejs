{ 
    "options" : {
    <% if (typeof mask !== "undefined") { %> 
        "mask": "<%- mask %>",
    <% } %>
    <% if (typeof promptChar !== "undefined") { %> 
        "promptChar": "<%- promptChar %>",
    <% } %>
    "value": "<%- defaultValue %>"
    }
}
{
    events: {
        onChange: function(e) {
            <% if (view.extension && events.onChange) { %>
                 $scope.factory['<%- events.onChange %>'](e);
            <% } %>
        }
    }
}